#include <stdio.h>
#include <math.h>

int main()
{
    int a, n;
    printf("enter the value : ");
    scanf("%d", &a);
    n = a / 10;

    printf("value of n : %d\n", n);

    return 0;
}